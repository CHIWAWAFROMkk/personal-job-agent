# 更新记录

## 1.0.0rc1（本地候选）— 2026-09-24

- 用户切换增加持久恢复记录，与岗位库事务绑定；进程中断、恢复再次中断后保留一致的资料、岗位与材料，无法确认时阻断读写。
- 首次使用支持无 API 的简历导入、JD 核对、准备、当前 PDF 审阅和本人明确补录；改进错误重试、键盘焦点和 PDF 单独打开入口。
- 修复同行 JD 标签解析与姓名误作经历；离线简历去除重复事实及内部占位文字，不推断未提供的机构、日期或在读状态。
- 桌面验收要求全部核心步骤实际通过，包含独立进程重启；发行版本从项目配置读取，源码、桌面目录及 ZIP 均执行隐私扫描。
- 新增首次使用、完整备份恢复和并排升级回退说明。真实用户试用、真实招聘反馈及物理断电不属于合成验收结论。

## 0.8.11（本地候选）— 2026-09-24

- 修复命令行岗位分析收到 OpenAI 或 DeepSeek 官方 401 后无法恢复额度：仅识别匹配器保留的原始 SDK 认证异常，释放该次本机预留，修正密钥后可重试。
- 超时、仅在错误文字中出现“401”及 DeepSeek 自定义服务地址继续按结果不明处理；复跑审查方的 CLI 合成复现与相关回归。
- 复核边界见 [0.8.11 源码交接](docs/SECURITY_REVIEW_HANDOFF_0.8.11.md)。

## 0.8.10（本地候选）— 2026-09-24

- 保留云端简历内容的事实待审来源：本地措辞修改、再次生成或导入不能仅凭最后一次引擎改为本地就清除核对要求；内容变更仍须重新检查当前版本。
- 切换用户后使旧工作台页面和浏览器扩展上下文失效；切换期间排队的旧写入返回 409，当前页面需刷新或使用切换响应中的新凭据。
- 明确收到 OpenAI、DeepSeek 的 401 认证拒绝时释放本机用量预留，修正密钥后可重试；超时与兼容服务的不确定结果继续保守占额。
- 复核边界见 [0.8.10 源码交接](docs/SECURITY_REVIEW_HANDOFF_0.8.10.md)。

## 0.8.9（本地候选）— 2026-09-24

- 修复云端成功响应后回退本地导致漏记用量；损坏用量记录暂停云端请求，桌面与 CLI 跨进程共享持久预留，超时或响应不明保留待核账名额。
- 新用户切换时先暂存资料并备份，在数据库事务中清理归档岗位、模拟面试等旧数据；旧简历、照片、聊天和浏览器会话整体归档，失败回滚，避免半切换与岗位编号复用泄露旧文件。
- 云端简历写作和润色须逐项核对原事实与改写，单独确认后才可批准；旧版缺少事实确认记录的云端简历需重新审核。损坏的简历清单隔离，不阻断其他岗位。
- 裸 UTC/GMT 日程不再误认作本地时间。实站 AI 自动代填暂停，保留本机模拟字段预览；桌面包不再携带未启用的 browser-use 依赖。
- 复核边界见 [0.8.9 源码交接](docs/SECURITY_REVIEW_HANDOFF_0.8.9.md)。

## 0.8.8（本地候选）— 2026-09-23

- 云端调用改为先预留本机额度、成功后结算；并发请求不会仅因计数滞后而越过同一进程的月度上限。损坏的用量记录会暂停新云端请求。
- 面试准备改为授权 POST；实验性 browser-use 在配置了 AI 月度上限时拒绝未计量的任务调用。
- 工作台增加短时缓存，并减少非实习岗位的无意义全文薪资扫描；合成大数据量基准和数据库初始化锁边界可复现。
- 桌面验收代码惰性移出常用入口；意外脚本错误有可操作提示，日程改按设备本地时区处理。
- 复核边界见 [0.8.8 源码交接](docs/SECURITY_REVIEW_HANDOFF_0.8.8.md)。

## 0.8.7（本地候选）— 2026-09-23

- 整份简历保存与润色请求可处理最多 128KB 内容；其他普通请求仍保持原有上限。
- 按任务设置明确的 AI 等待时间和零自动重试，避免长简历过早超时及重复付费请求。
- 同一桌面服务内并发写入 API 用量时不再丢计数；月度额度仍是本机提示，不承诺严格阻止并发超额。
- 切换 AI、搜索或地图服务后保留各自已保存的密钥；兼容 API 的密钥按服务地址隔离，明确删除时才清除所选密钥。
- 启动配置失败会显示原因；请求体错误返回客户端错误，超限请求在有界接收后给出明确提示。
- 桌面窗口验收仅允许隔离临时目录运行，避免示例身份进入真实资料；收紧脚本安全策略，补齐 DeepSeek 支持声明。

## 0.8.6（本地候选）— 2026-09-23

- 移除短信监听、同步接口、手动验证码中转及界面；保留用户历史数据，不主动清理旧配置。
- 首页改为建立资料、添加岗位、准备简历三步引导；空反馈不占位，辅助操作收进更多工具。
- 建档表单优先显示简历、目标岗位和城市；其他资料折叠，首次保存后回到下一步。
- 本地收录 Dream-RSI 固定版本研究资料及接入评估；作者尚未发布实现代码，未接入运行时。
- 大量岗位快照改为批量读取与匹配写入，移除 1000 条显示截断；职位列表按每页 60 条展示，支持跨页搜索和筛选。
- 进度页新增候选链接逐条核验与翻页，录入正式岗位前仍要求核对公司和完整岗位说明。
- 修复资料字段清空无效、切换新用户时沿用旧联系方式、误将刷新失败提示为保存失败，以及状态补录默认选中“已投递”的问题。

## 0.8.5 — 2026-09-20

- 恢复简历精修、通知登记与日历、模拟面试工作台的真实入口，支持双主题与窄屏。
- 恢复验证码手工录入与明确目标连接；移除代填预览中的默认成绩及未验证的成功承诺。
- 缺少档案时引导建立资料，岗位导入失败不再留下半成功记录；照片变为可选。
- 修复进度/资料页面被背景遮挡、移动端信息挤压与悬浮输入栏遮挡。
- JSON 响应声明准确长度，修复 Windows 未授权请求的连接中止。
- 收紧本地浏览器跨站请求边界；配对扩展仅可访问限定端点，不将 Origin 检查夸大为完整本机会话隔离。
- 修复代填弹窗迟到请求可能覆盖当前岗位任务的问题；看板读取不再阻塞其他页面刷新。
- 补齐公开 CI 所需离线基准脚本，并运行全部前端/扩展逻辑测试。
- 增加空用户实际流程验收脚本；所有测试采用隔离的合成数据。真实招聘网站和付费 AI 仍需用户配置后分别验证。

## 0.8.4 — 2026-09-19

- 安全与隐私全面加固：彻底清除前端模板、系统提示词和测试夹具中的硬编码个人身份信息，统一从本地 `/api/profile` 动态加载；验证码同步接口新增 PSK 令牌鉴权与本地端口隔离，阻断非授权窥探。
- SSRF 严格防护：新增 `safe_job_fetch`，锁定 DNS 解析至安全公网 IP，阻断内网与保留地址（IPv4/IPv6/NAT64 映射），支持严格 HTTPS 证书校验与重定向安全上限。
- 浏览器辅助安全收紧：检查异常即停止，禁用键盘快捷键、脚本、未知动作与通用点击。按钮由本人点击；功能仍属实验性，未完成真实站点副作用验收，不保证任意网站均可安全代填。
- PDF 从本机读取并嵌入可用中文 TrueType 字体；仓库不附带系统字体，已验证 Windows 输出，其他系统仍需验收。
- 经历选材保护：保留本地选中的受保护经历，云端遗漏时恢复；超过段落与要点预算时回退。并非无条件展示全部经历。
- 匹配度无污染重算：新增 `match_refresh` 指纹缓存，用户修改资料后即时触发推荐度刷新，同时严格保护既有投递状态链。
- 无障碍与视效标准（A11y & WCAG）：弹窗关闭按钮等交互元素全面适配 44px 最小触控目标，全面支持 `prefers-reduced-motion` 降动效标准。

## 0.8.3 — 2026-09-12

- 首页改为“今日”工作台，一次突出一个最值得推进的岗位及唯一主要操作。
- 显示薪资、通勤、截止时间和推荐依据；其余岗位折叠为后续队列。
- 将职位、投递进度、个人资料和 Agent 分离，减少同屏竞争与重复入口。
- 使用克制的系统蓝、清晰层级和平台原生交互，补齐桌面与窄屏布局。

- 统一单行短按钮、可见边框和禁用状态；优化中文字体和窄窗口布局。
- 明确 Agent 能力与人工操作边界，精简重复入口。
- 支持将单个失效岗位沉底、筛选和恢复，不影响同公司其他岗位。
- 招聘页无需先生成简历即可打开；生成简历不再自动弹出空白浏览器窗口。
- 云端简历修改失败时保留原稿并报告错误，不静默替换为简版。
- 保留公开版按用户 Profile 配置的求职策略，不附带个人数据。

## 0.8.2 — 2026-09-10

- 新增本机 Codex CLI 适配器，对话、JD 分析、简历导入与写作可复用本机登录。
- 根据完整已确认经历库生成整份 JD 定向简历，检查事实归属、量化数据和技能范围。
- 新增默认黑白 A4 模板，支持照片、经历说明与紧凑排版；导出检查 PDF 页数和文本完整性。
- 对话增加简历分析、待确认项和写作模板上下文；AI 写作失败时明确提示本地回退。
- 已投递岗位增加单独筛选、绿色边框和文字状态，在全部列表中分组置后。
- 缩短可选 Office 转换超时等待，为本地 PDF 回退和紧凑排版重试预留时间。
- 继续使用公开版按个人 Profile 配置的岗位策略，示例和测试使用合成数据。

Windows 用户下载本版本 ZIP 并完整解压运行；原有数据继续保存在用户自己的
`%LOCALAPPDATA%\PersonalJobAgent`。旧版发行包保留可下载。

## 0.8.1

首个公开版本：本地经历库、岗位匹配与搜索、定向简历、投递记录、通勤筛选、
项目工坊和面试准备。
